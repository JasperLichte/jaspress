{% include 'document.twig' %}

{% include '@common/header.twig' %}

{% for post in blog.posts %}
    <a href="{{ post.endpoint }}" class="post">
        <h3>{{ post.title }}</h3>
        <p>{{ post.markdown.content }}</p>
</a>
{% endfor %}

{% include 'html-end.twig' %}
